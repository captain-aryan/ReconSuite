========================
 ReconSuite
========================

ReconSuite is a lightweight automated web reconnaissance framework built for
offensive security testing.
It combines subdomain enumeration, directory
bruteforcing, web crawling, and vulnerability scanning (SQLi & XSS) into a
single workflow.

Usage:
./suite.sh <domain> <threads>

Example:
suite.sh testphp.vulnweb.com 100